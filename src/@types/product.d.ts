/* eslint-disable no-unused-vars */
export declare global {
  export interface product {
    id: string
    title: string
    description?: string
    price: number
    the_amount: number
    image?: string
  }
}
